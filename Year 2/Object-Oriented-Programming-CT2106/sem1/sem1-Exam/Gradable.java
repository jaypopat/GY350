/**
 * Write a description of interface Gradable here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public interface Gradable {
    boolean insertPercentage(int percentage);
}
